//Redirection
/*function sendUrl(url) {
	window.location.replace(url);
}

//Tool bar buttons
$(document).ready(function() {
	$('#delete').click(function() {
		var del = confirm("Are you sure you want to delete this?")
		 if (del == true){
		sendUrl('deletePersonalizedMessage.htm?messageId='+${personalizedMessage.messageId});
		alert("The Message with Message ID:"+${personalizedMessage.messageId}+" has been successfully Deleted.");
		 }
		 else{
			 alert("Current Operation Cancelled.");
		 }
	});
});*/